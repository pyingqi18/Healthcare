# Regression References

Entry `006` contains the first cross-sectional OLS and Logit models. Entry `008` introduces entity fixed effects. Entries `009a` through `013` add distance robustness, market-by-year effects, NLP outcomes, and event studies.

Entry `013` is the latest working notebook. Read the research-design audit before selecting a main specification.
